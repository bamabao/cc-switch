from pydantic_settings import BaseSettingsfrom functools import lru_cacheclass Settings(BaseSettings):    app_name: str = "爸妈�?    debug: bool = True    # Database    database_url: str = "sqlite:///./bamabao.db"    # JWT    secret_key: str = "change-me-in-production-bamabao-secret"    algorithm: str = "HS256"    access_token_expire_minutes: int = 60 * 24 * 7  # 7 days    # File storage (MinIO or local fallback)    storage_backend: str = "local"  # local | minio    upload_dir: str = "./uploads"    # Alert    alert_timeout_minutes: int = 30  # 未确认用�?-> 推送子�?    # Volcano Engine Vision    volcano_vision_endpoint: str = "https://ark.cn-beijing.volcengine.com/api/v3/chat/completions"    volcano_vision_model: str = "doubao-vision-v1"    volcano_vision_api_key: str = "YOUR_VOLCENGINE_API_KEY_CHANGE_ME"    volcano_timeout_seconds: int = 10    # OCR本地引擎置信度阈值（低于此值走火山视觉兜底�?    ocr_confidence_threshold: float = 0.6    # Reward    reward_per_dose: int = 10    reward_streak_7: int = 50    reward_streak_30: int = 200    class Config:        env_file = ".env"@lru_cache()def get_settings() -> Settings:    return Settings()