# 爸妈宝 — 环境变量配置示例

将此文件复制为 `backend/.env` 并根据实际环境修改。

```ini
# ============================================================
# 爸妈宝 后端配置
# 复制为 .env 文件后修改
# ============================================================

# ---- 应用 ----
APP_NAME=爸妈宝
DEBUG=true

# ---- 数据库 ----
# 开发环境（SQLite）
DATABASE_URL=sqlite:///./bamabao.db

# 生产环境（MySQL 8.0）
# DATABASE_URL=mysql+pymysql://bamabao:your-password@localhost:3306/bamabao

# ---- JWT ----
SECRET_KEY=change-me-in-production-bamabao-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ---- 文件存储 ----
STORAGE_BACKEND=local
UPLOAD_DIR=./uploads

# ---- 提醒配置 ----
ALERT_TIMEOUT_MINUTES=30

# ---- 积分奖励 ----
REWARD_PER_DOSE=10
REWARD_STREAK_7=50
REWARD_STREAK_30=200

# ---- GLM 智谱 AI（语音/图像分析） ----
GLM_API_KEY=your-glm-api-key-here
GLM_MODEL=glm-5.2-flash
```
