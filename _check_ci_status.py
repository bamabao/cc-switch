import requeststoken = 'YOUR_GITHUB_TOKEN'r = requests.get('https://api.github.com/repos/bamabao/cc-switch/actions/runs?per_page=5',    headers={'Authorization': f'token {token}'})data = r.json()if 'workflow_runs' not in data:    print('Error:', data)else:    for run in data['workflow_runs']:        sha = run['head_sha'][:7]        print(sha, run['status'], run['conclusion'], run['created_at'][:19], run['event'])